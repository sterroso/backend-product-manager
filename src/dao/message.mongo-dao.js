import MessageModel from "../models/message.model.js";

export const getMessages = async () => {};

export const getMessagesByUserId = async (userId) => {};

export const getMessage = async (messageId) => {};

export const getDeletedMessage = async (messageId) => {};

export const createMessage = async (messageObject) => {};

export const updateMessage = async (messageId, messageObject) => {};

export const restoreMessage = async (messageId) => {};

export const deleteMessage = async (messageId) => {};
